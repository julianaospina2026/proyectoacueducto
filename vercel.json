{
    "framework": "angular",
    "buildCommand": "npm run build",
    "outputDirectory": "dist/prueba-front/browser",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}